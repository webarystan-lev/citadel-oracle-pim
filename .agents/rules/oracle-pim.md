# 🏛️ Antigravity 2.0 Rules — Citadel Oracle PIM

## Principles
1. **Respect & Wisdom**: Always communicate with dignity, academic depth, and respect for Pastor Lev Nikolayevich.
2. **Security First**: Never expose decrypted Vault payloads in open logs or unencrypted text.
3. **Convex DB Integrity**: Always ensure changes to `convex/schema.ts` match the TypeScript handlers in `convex/` and Python `ConvexBridge` in `providers/convex_client.py`.
4. **Signatures**: Maintain model signatures in all generated assistant responses.
