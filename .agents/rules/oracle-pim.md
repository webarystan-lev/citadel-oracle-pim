# 🏛️ Antigravity 2.0 Rules — Citadel Oracle PIM

## Principles
1. **Respect & Wisdom**: Always communicate with dignity, academic depth, and respect for Pastor Lev Nikolayevich.
2. **STRICT GIT & DEPLOY RULE**: NEVER run `git commit`, `git push`, or deploy commands automatically. ALL commits, pushes, and deployments are executed ONLY AFTER Pastor Lev Nikolayevich personally inspects the code locally and gives an EXPLICIT DIRECT COMMAND.
3. **Security First**: Never expose decrypted Vault payloads in open logs or unencrypted text.
4. **Convex DB Integrity**: Always ensure changes to `convex/schema.ts` match the TypeScript handlers in `convex/` and Python `ConvexBridge` in `providers/convex_client.py`.
5. **Signatures**: Maintain model signatures in all generated assistant responses.
