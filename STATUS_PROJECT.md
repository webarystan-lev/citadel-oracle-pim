# 📊 STATUS_PROJECT.md — Свиток Состояния Проекта Citadel Oracle PIM

> **Дата Точки Останова (Breakpoint)**: 11 Августа 2026 г.
> **Состояние**: Актуализация провайдеров ИИ, живое получение динамических моделей, полное переписание README.md, синхронизация с Convex DB и зафиксированный коммит в GitHub (`origin main`).

---

## 🏛️ Достижения Сессии (11 Августа 2026 г.):

1. **Динамический запрос и интеграция моделей 3-х Провайдеров**:
   * **Google Gemini**: Полностью удалены устаревшие модели (`gemini-1.5-pro`, `gemini-2.0-flash-exp`). Исправлен вызов `list_available_gemini_models()` для поиска генеративных моделей по полю `supported_actions` в SDK `google.genai`.
   * **Anthropic Claude**: Подключена функция `list_available_anthropic_models()` для получения живых моделей от Anthropic по API-ключу пользователя.
   * **Mistral AI**: Функция `list_available_mistral_models()` доработана фильтрацией служебных не-чат эндпоинтов (embed, ocr, moderation, tts).
   * **Реестр Сигнатур**: Расширен словарь `CITADEL_SIGNATURES` с присвоением высоких чинов и рангов Цитадели для всех трех провайдеров.

2. **Детальная Документация Развертывания (`README.md`)**:
   * Файл `README.md` полностью переписан с нуля.
   * Описан пошаговый процесс: `git clone`, создание Python `.venv`, установка зависимостей из `requirements.txt`, файл переменный окружения `.env`.
   * Детально документирована установка, инициализация и подключение Convex DB (`npx convex dev` / `npx convex deploy`, полученный `CONVEX_URL`).
   * Задокументирован запуск приложения Streamlit (`streamlit run app.py`) и прохождение авторизации на экране Gatekeeper.

3. **Синхронизация и Публикация**:
   * Все свитки истории (`README.md`, `GEMINI.md`, `STATUS_PROJECT.md`) приведены в полное соответствие.
   * Проведен коммит и отправка изменений в Git-репозиторий `origin main`.

---

## 📋 План на Следующую Сессию (To-Do List):

1. **Доработка авто-переименования диалогов**:
   * Усовершенствовать логику авто-заголовка, чтобы при первом вопросе название чата мгновенно обновлялось по тексту запроса.
2. **Поэтапное тестирование модулей**:
   * Глубокая проверка взаимодействия модулей Журнала, Проектов, Заметок и Зашифрованного Сейфа.
